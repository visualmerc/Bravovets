@model ViewSyndicatedContentModel
@using bvResources = ProfSite.Resources.Resource

@for (int i = 0; i < Model.Content.Count; i += 0)
{
    Html.RenderPartial("Partials/_ResourceItem", Model.Content[i]);
    i++;
}